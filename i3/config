set $mod Mod4
set $nsi --no-startup-id
set $utils ~/utilities

font pango:IBM Plex Mono 10
for_window [class=".*"] title_format "  <span size='smaller' rise='1000'>%title</span>"
new_float normal 0

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

set $term terminology

# start a terminal
bindsym $mod+t exec $nsi $term

# kill focused window
bindsym $mod+q kill

# Scratchpad
bindsym $mod+minus scratchpad show
bindsym $mod+Tab scratchpad show
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+Shift+grave move scratchpad

bindsym $mod+d [class="terminology"] scratchpad show
bindsym $mod+grave [class="Google-chrome"] scratchpad show, resize set 1926 1053, move position -3 -3

bindsym $mod+Return exec --no-startup-id dmenu_extended_run

bindsym $mod+o exec $nsi "unset XDG_CURRENT_DESKTOP; unity-control-center"

# Changing Focus of windows
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Changing placement of windows
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
bindsym $mod+a focus child

bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

mode "Resize" {
	# Pressing left will shrink the window’s width.
	# Pressing right will grow the window’s width.
	# Pressing up will shrink the window’s height.
	# Pressing down will grow the window’s height.
	bindsym Left resize shrink width 10 px or 10 ppt
	bindsym Down resize shrink height 10 px or 10 ppt
	bindsym Up resize grow height 10 px or 10 ppt
	bindsym Right resize grow width 10 px or 10 ppt

	bindsym Return mode "default"
	bindsym Escape mode "default"
}

bindsym $mod+r mode "Resize"

# Status Bar (Replaced with Polybar)
#bar {
#	position bottom
#
#    status_command ~/.config/i3/i3status.sh
#
#	font pango:Ubuntu Mono 11
#	strip_workspace_numbers yes
#
#	colors {
#		background #0a0a0a
#	}
#}

# Workspaces
set $w1 "1:  Work"
set $w2 "2:  LaTeX"
set $w3 "3: ✍ Reading"
set $w4 "4:  Terminal"
set $w5 "5:  Nautilus"
set $w6 "6: ☕ Chill"
set $w7 "7: ♫ Art"
set $w8 "8: # Code"
set $w9 "9: ★ Extra"


# Switching Workspaces
bindsym $mod+1 workspace $w1
bindsym $mod+2 workspace $w2
bindsym $mod+3 workspace $w3
bindsym $mod+4 workspace $w4
bindsym $mod+5 workspace $w5
bindsym $mod+6 workspace $w6
bindsym $mod+7 workspace $w7
bindsym $mod+8 workspace $w8

bindsym $mod+b workspace $w9

# Additional commands to help switching to further workspaces
bindsym $mod+Mod1+1 workspace $w5
bindsym $mod+Mod1+2 workspace $w6
bindsym $mod+Mod1+3 workspace $w7
bindsym $mod+Mod1+4 workspace $w8

# Moving windows accross workspaces
bindsym $mod+Shift+1 move container to workspace $w1
bindsym $mod+Shift+2 move container to workspace $w2
bindsym $mod+Shift+3 move container to workspace $w3
bindsym $mod+Shift+4 move container to workspace $w4
bindsym $mod+Shift+5 move container to workspace $w5
bindsym $mod+Shift+6 move container to workspace $w6
bindsym $mod+Shift+7 move container to workspace $w7
bindsym $mod+Shift+8 move container to workspace $w8

bindsym $mod+Shift+b move container to workspace $w9

# Additional commands to help moving windows to further workspaces
bindsym $mod+Mod1+Shift+1 move container to workspace $w5
bindsym $mod+Mod1+Shift+2 move container to workspace $w6
bindsym $mod+Mod1+Shift+3 move container to workspace $w7
bindsym $mod+Mod1+Shift+4 move container to workspace $w8

# Traversing across open Workspaces
bindsym $mod+Control+Left workspace prev
bindsym $mod+Control+Right workspace next

# Windows' Border
for_window [class="^.*"] border pixel 2
hide_edge_borders smart

# Windows' Colors
client.focused 			#008b99 #008b99	#ffffff	#2e9ef4	#ffffff
client.focused_inactive	#484848	#484848	#ffffff	#484e50	#212121
client.unfocused       	#2f343f	#2f343f #ffffff	#292d2e	#212121
client.urgent          	#900000	#900000	#ffffff	#900000	#900000
client.placeholder      #000000	#0c0c0c	#ffffff	#000000	#0c0c0c

# Opening particular folders in Nautilus
mode "Nautilus" {
	bindsym i exec "nautilus ~/iitk/sem7"; mode "default"
	bindsym d exec "nautilus ~/dropbox"; mode "default"
	bindsym u exec "nautilus ~/utilities"; mode "default"
	bindsym p exec "nautilus ~/projects"; mode "default"
	bindsym m exec "nautilus ~/projects/machine-learning"; mode "default"
	bindsym b exec "nautilus ~/books"; mode "default"
	bindsym e exec "nautilus ~/"; mode "default"

    bindsym Return mode "default"
	bindsym Escape mode "default"
}

bindsym $mod+n mode "Nautilus"

# Chrome bindings
bindsym $mod+g exec "google-chrome-stable"
bindsym $mod+shift+g exec "google-chrome-stable --incognito"

# Assigning Workspaces to Classes
assign [class="Code"] $w8

# Power Settings
mode "System" {
    bindsym l exec --no-startup-id i3lock, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
	bindsym Shift+r exec shutdown -r now
    bindsym Shift+s exec shutdown now

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+shift+s mode System

# Tabbed settings
for_window [class = "Google-chrome"] layout tabbed
for_window [class = "Code"] layout tabbed

gaps inner 10
smart_gaps on

# Switching Windows
bindsym Mod1+Tab focus right
bindsym Mod1+shift+Tab focus left

# Show pavucontrol
bindsym $mod+x exec $nsi pavucontrol

# Audio Controls
bindsym XF86AudioRaiseVolume exec "volume set +1 +1"
bindsym XF86AudioLowerVolume exec "volume set -1 -1"

# Brightness controls
bindsym XF86MonBrightnessUp exec light -A 5
bindsym XF86MonBrightnessDown exec light -U 5

# Monitors
bindsym $mod+Shift+n exec $utils/general/screen/monitor.sh
bindsym $mod+Shift+h exec i3-msg move workspace to output left
bindsym $mod+Shift+j exec i3-msg move workspace to output right

# Launch components
exec_always "$utils/i3/launch.sh"

bindsym $mod+Shift+Control+Right exec i3-msg "move container to workspace next"
bindsym $mod+Shift+Control+Left exec i3-msg "move container to workspace prev"
